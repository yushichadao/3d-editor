# 立方·三维设计工坊 - 项目结构分析

> 本文档对项目整体结构进行梳理，包含 HTML 结构（L1–1000）与 JavaScript 结构（L1000–9500+）两大模块，逐区域说明其职责与关键代码位置。

---

## 一、HTML 结构（行 1–1000）

### 1.1 `<head>` (L1–515)：样式定义区

该区域集中存放所有 CSS 样式，包括全局变量、布局样式、控件样式、模态框样式及响应式适配。

| 子模块 | 行号 | 职责说明 |
| --- | --- | --- |
| CSS 变量 | L8–21 | 定义主题色系与全局尺寸变量：`--bg-deep`、`--bg-panel`、`--accent`、`--accent-2`、`--accent-3`、`--warn`、`--text`、`--text-dim`、`--border`、`--radius`、`--shadow` |
| 画布样式 | L32–36 | 定义 `#canvas-wrap`、`#three-canvas` 的尺寸与定位，承载 Three.js 渲染容器 |
| 顶栏样式 | L38–73 | `.topbar` 顶栏布局；`.brand` 品牌区；`.top-actions` 按钮组容器；`.top-btn` 单按钮样式；`.top-btn-sep` 分隔符 |
| 面板样式 | L75–100 | `.panel` 通用面板；`.panel-left` / `.panel-right` 左右抽屉；`.panel-toggle` 折叠按钮 |
| 形状网格样式 | L100–150 | `.shape-grid` 图标网格；`.shape-btn` 单个形状按钮的展示与悬停态 |
| 控件样式 | L150–300 | `.ctrl-row` / `.ctrl-label` 通用控件行；`.toggle-row2` / `.toggle-btn` 切换按钮组；`.color-row` 颜色行布局 |
| 底栏样式 | L300–400 | `.bottombar` 底栏容器；`.bb-toggle` 展开按钮；`.bb-body` 内容区；`.view-btn` 视角切换按钮 |
| 声明样式 | L400–450 | `.declaration` 声明容器；`.decl-header` / `.decl-body` 头部与正文 |
| 模态框样式 | L450–500 | `.modal-mask` 遮罩层；`.modal-box` 弹窗主体 |
| 响应式媒体查询 | L500–515 | `@media (max-width:768px)` 平板适配；`@media (max-width:460px)` 手机适配 |

### 1.2 `<body>` (L517–1000)：页面结构区

#### 加载层 (L519)
- `.loader`：首屏加载动画，待场景初始化完成后隐藏。

#### 画布区域 (L522–523)
- `#canvas-wrap`：画布外层容器
- `#three-canvas`：Three.js 渲染目标
- `#box-sel`：选择框辅助元素

#### 顶栏 (L526–545)
`.topbar` 内含 `.brand` 与 14 个 `.top-btn`，按功能分组：

| 分组 | 包含按钮 |
| --- | --- |
| 文件操作 | 新建 / 导入 / 导出 / 截图 |
| 编辑操作 | 撤销 / 重做 / 复制 / 粘贴 / 克隆 / 清空 |
| 视图辅助 | 坐标轴 / 网格面 / 全屏 |

#### 左面板 (L551–615)
共 4 个可折叠区域：

1. **3D 图形** (`sec-3d`)：`#grid-3d`，承载 3D 形状按钮网格
2. **2D 图形** (`sec-2d`)：`#grid-2d`，承载 2D 形状按钮网格
3. **工具** (`sec-tools`)：选择/变换、3D 画笔、2D 画笔、橡皮擦、添加文本、添加图片
4. **变换模式** (`sec-transform`)：平移 / 旋转 / 缩放 + 等比缩放锁

#### 右面板 (L618–924)
多个可折叠区域，按对象属性维度组织：

| 区域 | 标识 | 内容 |
| --- | --- | --- |
| 属性 | `sec-props` | X/Y/Z 坐标、水平/垂直/侧向旋转、等比缩放、透明度、图形参数 |
| 颜色 | `sec-color` | 27 色预设 + 自定义颜色 + 选取器 |
| 花纹/贴图 | `sec-pat` | 12 种花纹 + 上传图片 + 清除贴图 + 编辑文本 |
| 文本框 | `sec-text` | 字体 / 字号 / 粗细 / 排列方向 |
| 删除 | `sec-del` | 删除按钮 + 多选提示 |
| 画笔属性 | `sec-brush` | 颜色 / 粗细 / 透明度 / 最小间距 |
| 橡皮擦属性 | `sec-eraser` | 大小 / 2D 笔迹局部擦除复选框 |
| 鼠标操作 | `sec-mouse` | 鼠标操作说明（6 项） / 触摸手势说明（7 项） |
| 键盘快捷键 | `sec-kbd` | 基础按键 / Shift 组合键 / 滑动条输入框 / 功能键 / Ctrl 组合键 |

#### 声明 (L927–941)
- `.declaration`：项目声明文本区。

#### 底栏 (L944–957)
- `.bottombar`：视角切换按钮组，包含 透视 / 顶 / 底 / 正 / 后 / 左 / 右 共 7 个视角。

#### 颜色选取器弹窗 (L962–1010)
- HSV 面板 + 色相条 + HEX / RGB 输入框，作为自定义颜色选择浮层。

---

## 二、JavaScript 结构（行 1000–9500+）

### 2.1 全局变量与常量 (L1000–2020)

负责场景核心对象、工厂函数集合、图标资源、预设数据与全局状态的初始化。

| 名称 | 行号 | 说明 |
| --- | --- | --- |
| 场景初始化 | L1000 起 | `scene`、`camera`、`renderer`、`controls`、`raycaster` 等 Three.js 核心对象 |
| `SHAPES_3D` | L1621–1877 | 20 种 3D 图形工厂函数集合 |
| `SHAPES_2D` | L1878–1899 | 20 种 2D 图形工厂函数集合 |
| `ICONS` | L1901–1943 | SVG 图标定义库 |
| `COLORS` | L1945–1956 | 27 种预设颜色 |
| `PATTERNS` | L1959 | 12 种花纹名称列表 |
| `state` | L1962–1987 | 全局状态对象（选择、模式、历史等） |
| `colorMemory` | L1989–1995 | 颜色记忆系统，记录对象颜色以便恢复 |
| `brush` | L2010–2020 | 画笔状态（颜色、粗细、透明度、间距等） |

### 2.2 功能模块划分

#### 模块 1：坐标轴与参考平面 (L1176–1290)
- **职责**：构建场景坐标轴、参考网格平面，提供空间定位与对齐辅助。
- **关键代码**：坐标轴对象构造、网格面渲染控制、显隐切换逻辑。

#### 模块 2：幽灵预览系统 (L1301–1360)
- **职责**：在鼠标悬停或放置前显示半透明预览对象，辅助用户判断落点。
- **关键代码**：幽灵对象创建与跟随、显隐与销毁。

#### 模块 3：选择与变换 (L1406–1614)
- **职责**：处理对象选中、变换控制器（平移/旋转/缩放）绑定与模式切换。
- **关键代码**：`TransformControls` 绑定、模式切换、等比缩放锁逻辑。

#### 模块 4：渲染顺序管理 (L2022–2055)
- **职责**：管理透明对象与不透明对象的渲染排序，避免透明叠加渲染异常。
- **关键代码**：`renderOrder` 设置、透明度分组排序。

#### 模块 5：颜色与纹理工具 (L2056–2265)
- **职责**：对象颜色应用、贴图加载、花纹生成与材质更新。
- **关键代码**：纹理加载器、花纹 Canvas 生成、材质属性同步。

#### 模块 6：对象创建 (L2266–2434)
- **职责**：根据形状工厂创建 3D/2D 对象并入场景。
- **关键代码**：工厂调用、默认材质与参数注入、场景挂载。

#### 模块 7：选择系统 (L2436–2594)
- **职责**：单选/多选、选择高亮、选择集维护。
- **关键代码**：射线选择回调、选择集增删、高亮边框绘制。

#### 模块 8：属性面板 (L2594–2827)
- **职责**：将选中对象属性同步至右面板输入控件，并响应用户修改回写对象。
- **关键代码**：面板刷新函数、输入框双向绑定、滑块联动。

#### 模块 9：历史记录 (L2827–3376)
- **职责**：撤销/重做栈管理，记录对象增删改快照。
- **关键代码**：快照序列化、栈推入/弹出、状态恢复。

#### 模块 10：射线检测 (L3377–3568)
- **职责**：鼠标/触摸点投射射线，判定命中对象与命中点。
- **关键代码**：`Raycaster` 配置、命中过滤、交点提取。

#### 模块 11：文本输入 (L3644–3792)
- **职责**：添加文本对象，弹出输入框并生成文字纹理/几何。
- **关键代码**：文本输入弹窗、字体参数应用、文字纹理生成。

#### 模块 12：工具切换 (L3793–3927)
- **职责**：在 选择/变换、3D 画笔、2D 画笔、橡皮擦、文本、图片 等工具间切换。
- **关键代码**：工具激活态管理、控件模式切换、光标样式更新。

#### 模块 13：图形网格构建 (L3928–4084)
- **职责**：根据 `SHAPES_3D` / `SHAPES_2D` 动态生成左面板形状按钮网格。
- **关键代码**：图标渲染、按钮事件绑定、网格布局填充。

#### 模块 14：对象放置 (L4085–4185)
- **职责**：点击画布后将当前形状放置到命中点，处理吸附与偏移。
- **关键代码**：放置位置计算、吸附判定、对象入栈。

#### 模块 15：颜色选择器 (L4185–4642)
- **职责**：实现 HSV 拾色器弹窗，支持色相条、HEX/RGB 互转与回调。
- **关键代码**：HSV↔RGB↔HEX 转换、面板交互、选取结果回调。

#### 模块 16：画笔系统 (L4643–6200)
- **职责**：3D/2D 画笔绘制，沿鼠标轨迹生成笔迹几何。
- **关键代码**：轨迹采样、最小间距过滤、笔迹几何生成与材质应用。

#### 模块 17：橡皮擦系统 (L6202–7325)
- **职责**：擦除对象或 2D 笔迹局部，支持大小调节与局部擦除。
- **关键代码**：擦除范围检测、2D 笔迹局部擦除、对象整体删除判定。

#### 模块 18：擦除调度 (L7326–7434)
- **职责**：协调橡皮擦与选择系统、历史记录的交互流程。
- **关键代码**：擦除任务编排、历史快照推送、选择集同步。

#### 模块 19：图形参数面板 (L7436–8002)
- **职责**：根据选中对象类型动态生成参数控件（如半径、高度、分段数等）。
- **关键代码**：参数 schema 构建、控件动态渲染、参数变更回写。

#### 模块 20：复制粘贴克隆 (L8003–8960)
- **职责**：对象复制到剪贴板、粘贴、克隆，含位置偏移与属性复制。
- **关键代码**：序列化剪贴板、粘贴偏移计算、克隆对象重建。

#### 模块 21：视角与网格 (L8960–9450)
- **职责**：7 个预设视角切换（透视/顶/底/正/后/左/右）与网格面显隐控制。
- **关键代码**：相机位置/朝向插值、视角按钮激活态、网格面显隐。

#### 模块 22：动画循环 (L9452–9500)
- **职责**：主渲染循环，驱动场景刷新与控制器更新。
- **关键代码**：`requestAnimationFrame` 调度、`controls.update()`、`renderer.render()`。

#### 模块 23：事件绑定 (L9500–9800+)
- **职责**：集中注册 DOM 与画布事件监听，串联各模块交互。
- **关键代码**：鼠标/触摸/键盘事件分发、控件事件绑定、初始化入口。

---

## 三、整体职责概览

- **HTML 部分**：以"顶栏 + 左面板 + 画布 + 右面板 + 底栏"五区布局组织界面，配合弹窗与响应式样式，覆盖形状库、工具、属性、颜色、贴图、文本、画笔、橡皮擦、快捷键等完整编辑能力。
- **JavaScript 部分**：以"全局状态 + 23 个功能模块"分治实现，从场景初始化、对象创建、选择变换、属性同步、历史记录，到画笔/橡皮擦绘制、颜色拾取、复制粘贴、视角切换与动画循环，形成完整的三维编辑闭环。
