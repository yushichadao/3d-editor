# 立方·三维设计工坊 使用说明书

> **从入门到精通的完全指南**
>
> 作者：yushichadao
> 技术基础：Three.js 纯前端 3D 编辑器
> 访问方式：在浏览器中打开 https://yushichadao.github.io/3d-editor/

---

## 目录

- [前言](#前言)
- [第一章 入门基础](#第一章-入门基础)
- [第二章 放置图形](#第二章-放置图形)
- [第三章 选择与变换](#第三章-选择与变换)
- [第四章 颜色与花纹](#第四章-颜色与花纹)
- [第五章 画笔工具](#第五章-画笔工具)
- [第六章 橡皮擦](#第六章-橡皮擦)
- [第七章 文本和图片](#第七章-文本和图片)
- [第八章 视角控制](#第八章-视角控制)
- [第九章 键盘快捷键](#第九章-键盘快捷键)
- [第十章 复制、克隆与历史](#第十章-复制克隆与历史)
- [第十一章 文件操作](#第十一章-文件操作)
- [第十二章 高级技巧](#第十二章-高级技巧)
- [附录 A 常见问题](#附录-a-常见问题)
- [附录 B 速查表](#附录-b-速查表)

---

## 前言

欢迎使用 **立方·三维设计工坊**！这是一款运行在浏览器中的纯前端三维设计工具。无需安装任何软件，无需注册登录，打开网页即可开始你的三维创作之旅。

本说明书面向 **完全没有任何使用经验的初学者**，采用教材式的编写方式，由浅入深、循序渐进。只要你按照章节顺序阅读并动手实践，就能从一无所知的新手，成长为能够熟练创作三维作品的设计达人。

### 谁适合阅读本书

- 从未使用过三维软件的零基础用户
- 想快速上手进行简单三维拼搭、绘画、装饰的爱好者
- 需要进行教学演示、空间规划、概念草图设计的用户
- 喜欢在浏览器中即开即用、不愿安装庞大软件的用户

### 阅读建议

1. **第一遍**：按章节顺序通读，每读完一节就在软件中实际操作一次。
2. **第二遍**：把自己当作"出题人"，尝试用学过的功能组合出一个小作品。
3. **遇到问题**：先查阅 [附录 A 常见问题](#附录-a-常见问题)，再回到对应章节复习。

### 一句话承诺

> 你不需要懂三维数学，不需要懂代码，也不需要懂 Three.js。你只需要会点鼠标、会按键盘，就能用本工具创造属于你的三维世界。

---

## 第一章 入门基础

本章将带你认识软件的全貌，学会最基本的鼠标与触摸操作。这是后续所有章节的基础，请务必动手练习。

### 1.1 打开网站后的初始界面

1. 在浏览器地址栏输入：

   ```
   https://yushichadao.github.io/3d-editor/
   ```

2. 按下回车，稍等片刻，你将看到如下初始画面：
   - 中央是一片带网格的灰色"地面"，这就是你的 **3D 画布**。
   - 地面上有红、绿、蓝三根相互垂直的线段，这是 **坐标轴**。
   - 四周分布着若干工具面板，等待你的指令。
   - 画面默认是空的，没有任何物体——你的创作从这里开始。

> 💡 **小贴士**：如果你看到的是空白页或报错，请确认网络连通状态。不同浏览器均可使用，推荐 Chrome 或 Edge 以获得最佳体验。

### 1.2 认识界面布局

整个界面由 **六大区域** 组成，请对照下图位置一一记住它们的名字：

```
┌─────────────────────────────────────────────────────────┐
│  顶栏：Logo + 按钮区（可横向滚动）                          │  ← 最上方
├──────┬────────────────────────────────────┬─────────────┤
│      │                                    │             │
│ 左   │                                    │   右面板     │
│ 面板 │            画布（3D 场景）          │             │
│      │                                    │             │
├──────┴────────────────────────────────────┴─────────────┤
│  底栏：视角切换按钮（可折叠）                               │  ← 最下方
├─────────────────────────────────────────────────────────┤
│  声明栏                                                   │
└─────────────────────────────────────────────────────────┘
```

#### 1.2.1 顶栏（屏幕最上方）

顶栏包含品牌 Logo 和若干按钮，按钮按功能分组排列。当窗口较窄、按钮显示不全时，可以 **横向滚动** 查看更多按钮。

按钮分组如下：

| 分组 | 包含按钮 | 作用 |
| --- | --- | --- |
| 文件操作 | 新建 / 导入 / 导出 / 截图 | 管理场景文件 |
| 编辑历史 | 撤销 / 重做 / 复制 / 粘贴 / 克隆 / 清空 | 编辑与历史记录 |
| 显示设置 | 坐标轴 / 网格面 / 全屏 | 切换辅助显示 |

> ⚠️ **注意**：滚动顶栏时请尽量在 **非按钮区域** 滑动滚轮，否则可能误触按钮。顶栏的滚轮缩放功能只在非按钮区生效。

#### 1.2.2 左面板

左面板是你"取用素材"和"切换工具"的地方，自上而下分为三大区：

1. **3D 图形区**：提供 20 种三维立体图形（如立方体、球体、圆柱等）。
2. **2D 图形区**：提供 20 种二维平面图形（如方形、圆形、三角形等）。
3. **工具区**：
   - 选择 / 变换
   - 3D 画笔
   - 2D 画笔
   - 橡皮擦
   - 添加文本
   - 添加图片
4. **变换模式**（在选择/变换工具激活时出现）：
   - 平移 / 旋转 / 缩放 三种模式
   - 等比缩放锁（开关）

#### 1.2.3 右面板

右面板是"设置属性"的地方，会根据你当前选中的对象或工具显示不同的内容。常见的分区有：

- **属性区**：X / Y / Z 坐标、旋转角度、缩放比例、透明度
- **颜色区**：27 种预设颜色 + 自定义颜色 + HSV 选取器
- **花纹 / 贴图区**：12 种花纹 + 上传图片贴图
- **文本框区**：字体 / 字号 / 粗细 / 排列方向
- **画笔属性区**：颜色 / 粗细 / 透明度 / 最小间距
- **橡皮擦属性区**：大小 / 2D 笔迹局部擦除开关
- **鼠标操作说明区**：快速查阅鼠标用法
- **键盘快捷键区**：快速查阅快捷键

#### 1.2.4 底栏（屏幕最下方）

底栏提供 7 种预设视角切换按钮：

- **透视**（默认的立体视角）
- **顶视 / 底视**（从正上方 / 正下方看）
- **正视 / 后视**（从正前方 / 正后方看）
- **左视 / 右视**（从正左侧 / 正右侧看）

底栏可折叠，点击折叠按钮可将其收起以扩大画布空间。

#### 1.2.5 画布

画布是屏幕中央的 3D 场景区域，是你创作的"舞台"。所有的图形、笔迹、文字、图片都会显示在这里。

#### 1.2.6 声明栏

屏幕最底部有一行声明文字，说明软件的版权与免责信息。

### 1.3 坐标系说明

三维空间用三根互相垂直的轴来表示位置，本软件采用如下约定：

| 轴 | 颜色 | 方向 | 含义 |
| --- | --- | --- | --- |
| **X 轴** | 红色 | 左右方向 | 向右为正，向左为负 |
| **Y 轴** | 绿色 | 前后方向 | 向前（靠近你）为正，向后为负 |
| **Z 轴** | 蓝色 | 上下方向 | 向上为正，向下为负 |

> 📌 **重要**：地面为 **XY 平面**，所有对象默认放置在 **Y = 0** 的高度（即贴在地面上）。Z 轴代表"高度"。理解这一点，对你后续精确摆放对象至关重要。

> 💡 **记忆口诀**："红左右、绿前后、蓝上下；地面是 XY，高度看 Z。"

### 1.4 鼠标基本操作

鼠标是你最常用的输入设备，请牢记以下基本手势：

| 操作 | 用途 |
| --- | --- |
| **单击左键** | 选择对象 / 点击控件 / 激活按钮 |
| **双击左键** | 编辑文本对象（详见第七章） |
| **长按左键并拖动** | 操作对象 / 拖动控件 / 框选 |
| **长按右键并拖动** | 旋转视角（环绕场景观察） |
| **长按滚轮（中键）并拖动** | 平移视角（场景左右上下移动） |
| **滑动滚轮** | 缩放视角（拉近 / 推远） |

> 🧪 **练习 1.1**：在空场景中，按住右键拖动，观察视角如何旋转；按住滚轮拖动，观察视角如何平移；滑动滚轮，观察场景如何放大缩小。反复练习几次，直到手感熟练。

### 1.5 触摸设备手势

如果你使用的是触摸屏笔记本、平板或手机，可以用手势代替鼠标：

| 手势 | 用途 |
| --- | --- |
| **单指单击** | 选择对象 / 点击控件 |
| **单指双击** | 编辑文本 |
| **单指拖动对象** | 操作对象 / 控件 |
| **单指拖动空白处** | 旋转视角 |
| **单指轻点空白处** | 取消选择 |
| **双指拖动** | 平移视角 |
| **双指捏合 / 张开** | 缩放视角 |

> ⚠️ **注意**：触摸操作与鼠标操作略有差异。例如"单指拖动空白处"在触摸设备上用于旋转视角，而鼠标则是"右键拖动"旋转视角。请根据设备选择对应的操作方式。

---

## 第二章 放置图形

本章教你如何把第一个三维对象放进画布，并调整它的位置。这是创作的第一步。

### 2.1 从左面板选择 3D 图形

1. 在左面板的 **3D 图形区** 找到你想要的图形，例如"立方体"。
2. **单击** 该图形按钮，按钮会高亮显示，表示它已被选中。
3. 此时鼠标进入"放置模式"——下一步你只需要在画布上点一下即可放置。

> 💡 **小贴士**：3D 图形区共有 20 种立体图形，包括立方体、球体、圆柱、圆锥、圆台、棱柱、棱锥、圆环、管道、胶囊、半球、十字等。把鼠标悬停在按钮上停留片刻，通常会显示名称提示。

### 2.2 在画布上点击地面放置

1. 把鼠标移到画布中央的灰色地面上。
2. 在你希望放置的位置 **单击左键**。
3. 一个立方体就会"啪"地一声出现在地面上，并自动落地。

> 📌 **自动落地**：所有图形放置时都会 **自动让底部贴在 XY 平面（Y=0）**。你不必担心图形悬空或埋入地下。

### 2.3 长按拖动调整位置

1. 在放置后，图形会保持被选中状态。
2. 切换到 **选择 / 变换** 工具（若未自动切换）。
3. 在 **平移模式** 下，用鼠标 **长按图形并拖动**，即可在地面上自由移动它。
4. 拖动时图形会跟随鼠标移动，松开鼠标即停止。

### 2.4 放置 2D 图形

放置 2D 图形与 3D 图形完全同理：

1. 在左面板的 **2D 图形区** 选择一种图形（如圆形）。
2. 在画布上单击地面放置。
3. 2D 图形默认平铺在 XY 地面上，像一张贴纸。

> 🧪 **练习 2.1**：依次放置一个立方体、一个球体和一个圆形，用拖动的方式把它们排列成一排。

### 2.5 图形自动落地详解

无论你从哪个视角放置图形，软件都会：

1. 计算图形的最低点；
2. 将最低点对齐到 Y = 0 的地面；
3. 让图形稳稳"站"在地面上。

这意味着：你可以在顶视图中放心放置，图形也会落到地面，而不会漂浮在空中。

---

## 第三章 选择与变换

本章是全书的核心。学会选择对象并对它进行平移、旋转、缩放，你才能精确地塑造场景。

### 3.1 激活"选择 / 变换"工具

1. 在左面板的 **工具区** 点击"选择 / 变换"按钮。
2. 按钮高亮表示已激活。
3. 此时鼠标在画布上变成"选择"状态。

### 3.2 单击选中对象

1. 用鼠标 **单击** 任意对象。
2. 被选中的对象会显示一个 **彩色变换控件**（由红、绿、蓝三个箭头或圆环组成）。
3. 同时，右面板会显示该对象的属性。

### 3.3 Shift + 点击多选

1. 先单击选中第一个对象。
2. 按住键盘上的 **Shift** 键，再单击第二个、第三个对象……
3. 所有被选中的对象会同时高亮，组成一个"选择组"。
4. 再次 Shift + 点击已选中的对象，可将其从选择组中移除。

### 3.4 框选

1. 在 **画布空白处**（不要点到任何对象）按下鼠标左键。
2. 拖动鼠标，会出现一个矩形选择框。
3. 松开鼠标，框内的所有对象都会被选中。

> 💡 **小贴士**：框选非常适合一次选中一片区域内的多个对象，比如同时移动一排树木。

### 3.5 平移模式

平移（Translation）就是改变对象在空间中的位置。

**方法 A：拖动控件**

1. 在左面板的变换模式中选择 **平移**。
2. 被选中对象周围会出现三根彩色箭头：
   - 红箭头：沿 X 轴（左右）
   - 绿箭头：沿 Y 轴（前后）
   - 蓝箭头：沿 Z 轴（上下）
3. 长按某根箭头并拖动，对象只沿该轴方向移动。

**方法 B：修改属性面板**

1. 在右面板的 **属性区** 找到 X / Y / Z 三个数值。
2. 直接输入数字或拖动数值滑块，对象会精确移动到该坐标。

### 3.6 旋转模式

旋转（Rotation）让对象绕轴转动。

**方法 A：拖动控件**

1. 在变换模式中选择 **旋转**。
2. 被选中对象周围会出现三根彩色圆环：
   - 红环：绕 X 轴旋转（前后翻转）
   - 绿环：绕 Y 轴旋转（水平转身）
   - 蓝环：绕 Z 轴旋转（左右翻倒）
3. 长按某根圆环并拖动，对象绕该轴旋转。

**方法 B：修改属性面板**

1. 在右面板属性区找到 **水平旋转 / 垂直旋转 / 侧向旋转** 三个数值。
2. 输入角度（单位：度），对象会精确旋转。

### 3.7 缩放模式

缩放（Scale）改变对象的大小。

**方法 A：拖动控件**

1. 在变换模式中选择 **缩放**。
2. 被选中对象周围会出现三根带方块的彩色手柄。
3. 长按某根手柄并拖动，可沿该轴方向单独缩放。

**方法 B：修改属性面板**

1. 在右面板属性区找到 **等比缩放** 数值。
2. 输入缩放倍数（1 = 原大小，2 = 放大两倍，0.5 = 缩小一半）。

### 3.8 等比缩放锁

- 当 **等比缩放锁** 打开时：拖动任意手柄，对象在三个方向上 **同步** 缩放，形状不变形。
- 当锁关闭时：可以沿单轴单独缩放，对象会被拉长或压扁。

> 📌 **建议**：保持对象原有比例时打开锁；想做出"瘦高""扁平"等特殊造型时关闭锁。

### 3.9 多选时整体变换

当你选中多个对象时：

- 拖动控件或修改属性，**所有选中对象会作为一个整体** 同时变换。
- 平移时各对象保持相对位置一起移动。
- 旋转 / 缩放时以"选择组中心"为基准进行变换。

> 🧪 **练习 3.1**：放置三个立方体，用框选把它们全部选中，然后整体旋转 45°，观察它们如何一起转动。

---

## 第四章 颜色与花纹

本章教你为对象上色、添加花纹和贴图，让你的作品告别单调的灰白色。

### 4.1 选中对象后右面板显示颜色区

1. 用选择工具单击选中一个对象。
2. 右面板会自动切换到该对象的属性，其中 **颜色区** 位于显眼位置。
3. 颜色区上方是 27 种预设色块，下方是自定义颜色工具。

### 4.2 27 种预设颜色

颜色区展示 **27 种预设颜色**，按色相分组排列，覆盖常见的红、橙、黄、绿、青、蓝、紫、黑、白、灰等。

- **单击** 任一色块，对象立即变为该颜色。
- 颜色更改会即时反映在画布上。

### 4.3 自定义颜色

如果你想用预设之外的颜色：

**方法 A：HEX 输入**

1. 在颜色区找到 HEX 输入框（形如 `#FF8800`）。
2. 输入 6 位十六进制颜色代码，回车确认。

**方法 B：颜色选择器**

1. 点击颜色预览框，弹出系统颜色选择器。
2. 在选择器中拖动滑块或点击色板选色，确认即可。

### 4.4 HSV 颜色选取器

HSV 选取器提供更直观的选色方式，由三部分组成：

| 部分 | 作用 |
| --- | --- |
| **色相条** | 一条彩色的长条，拖动滑块选择"基础颜色"（红橙黄绿青蓝紫一圈） |
| **饱和度 / 明度面板** | 一个方形色板，左右拖动调饱和度，上下拖动调明度 |
| **RGB 输入** | 直接输入 R / G / B 三个 0~255 的数值 |

**使用步骤**：

1. 先在色相条上选定大致的颜色（例如选"红"）。
2. 再在饱和度 / 明度面板上拖动，调整鲜艳度和亮度。
3. 也可以直接在 RGB 输入框中输入精确数值。

### 4.5 12 种花纹

花纹区提供 **12 种预设花纹**，可一键应用到对象表面：

| 序号 | 花纹名 | 效果描述 |
| --- | --- | --- |
| 1 | 方格 | 棋盘格图案 |
| 2 | 条纹 | 横向或纵向条纹 |
| 3 | 圆点 | 规则排列的圆点 |
| 4 | 渐变 | 由浅到深的渐变 |
| 5 | 砖墙 | 类似红砖墙的拼接 |
| 6 | 斜纹 | 倾斜的条纹 |
| 7 | 波浪 | 起伏的波浪线 |
| 8 | 点阵 | 细密的小点矩阵 |
| 9 | 交叉 | 交叉十字图案 |
| 10 | 网格 | 细密的网格线 |
| 11 | 螺纹 | 螺旋状纹路 |
| 12 | （扩展花纹） | 其它预设图案 |

**使用方法**：选中对象 → 在花纹区单击所需花纹 → 对象表面立即出现该花纹。

### 4.6 上传图片作为贴图

你可以把任意图片"贴"到对象表面：

1. 选中对象。
2. 在花纹 / 贴图区找到 **上传图片贴图** 按钮。
3. 点击按钮，选择本地图片文件（推荐 JPG / PNG）。
4. 上传后，图片会作为贴图覆盖在对象表面。

> 💡 **小贴士**：正方形图片贴在立方体上效果最佳；贴图会自动适配对象表面。

### 4.7 清除花纹 / 贴图

如果想恢复纯色：

1. 选中已有花纹 / 贴图的对象。
2. 在花纹 / 贴图区点击 **清除** 按钮。
3. 花纹 / 贴图消失，对象恢复为当前基础颜色。

### 4.8 透明度调节

1. 选中对象。
2. 在右面板属性区找到 **透明度** 滑块（范围 0~100%）。
3. 拖动滑块：
   - 100% = 完全不透明
   - 50% = 半透明
   - 0% = 完全透明（看不见）

> ⚠️ **注意**：透明对象在渲染时可能存在排序问题，多个半透明对象重叠时可能出现显示异常，详见第十二章。

### 4.9 无颜色模式

如果你想取消对象的颜色（让它显示为默认材质或完全不渲染表面）：

1. 选中对象。
2. 在颜色区点击 **无颜色** 按钮（或取消颜色选择）。
3. 对象将不再显示自定义颜色。

> 🧪 **练习 4.1**：放置一个立方体，依次应用方格、条纹、砖墙三种花纹，再上传一张你喜欢的图片作为贴图，最后清除贴图恢复纯色。

---

## 第五章 画笔工具

本章教你用"画笔"在场景中自由绘制线条。画笔分两种：**2D 画笔** 在地面上画，**3D 画笔** 在空间中画。

### 5.1 2D 画笔

2D 画笔在 **XY 平面（地面）** 上绘制平面笔迹，适合画地面上的图案、文字底纹、路径示意等。

#### 5.1.1 设置画笔属性

激活 2D 画笔后，右面板会显示画笔属性：

| 属性 | 作用 |
| --- | --- |
| **颜色** | 笔迹的颜色（用法同对象颜色） |
| **粗细** | 笔迹的宽度（数值越大越粗） |
| **透明度** | 笔迹的不透明度 |
| **最小间距** | 两个采样点之间的最小距离，影响笔迹平滑度 |

> 💡 **小贴士**：最小间距过大笔迹会变"折线"，过小会变卡顿。一般保持默认值即可。

#### 5.1.2 在画布上拖动绘制

1. 激活 2D 画笔。
2. 在画布上 **按下左键**。
3. **拖动** 鼠标，笔迹会跟随鼠标在地面上延伸。
4. **松开** 左键，完成一笔。

#### 5.1.3 Shift + 拖动约束直线

1. 按下左键开始绘制。
2. 按住 **Shift** 键继续拖动。
3. 笔迹会被约束为 **一条直线**（沿起点到当前鼠标的方向）。
4. 松开左键完成直线。

> 📌 **注意**：一笔之内只能约束一条直线，松开 Shift 或重新起笔才能改变方向。

### 5.2 3D 画笔

3D 画笔在 **3D 空间** 中绘制立体的"管道"，适合画立体的曲线、装饰边框、立体签名等。

#### 5.2.1 设置画笔属性

激活 3D 画笔后，右面板显示：

| 属性 | 作用 |
| --- | --- |
| **颜色** | 管道颜色 |
| **粗细** | 管道直径 |
| **透明度** | 管道不透明度 |

#### 5.2.2 首次点击确定参考平面

3D 画笔需要先确定一个"参考平面"才能绘制：

1. 激活 3D 画笔。
2. 在画布上 **单击一次**，会在场景中显示一个半透明的参考平面。
3. 这个平面通常平行于当前视角，作为后续绘图的"画板"。

> 💡 **小贴士**：如果你想画一条水平方向的立体曲线，可以先把视角调整为顶视，再点击确定参考平面。

#### 5.2.3 在参考平面上拖动绘制

1. 参考平面确定后，**按下左键**。
2. 在参考平面上 **拖动**，管道会沿着鼠标轨迹生成。
3. **松开** 左键完成一笔。
4. 下一笔会沿用同一参考平面，直到你重新点击确定新平面。

#### 5.2.4 Ctrl + 拖动约束直线方向

1. 按下左键开始绘制。
2. 按住 **Ctrl** 键继续拖动。
3. 管道被约束为 **一条直线**。
4. 松开左键完成。

> ⚠️ **重要**：一笔之内 **只能约束一条直线**，即一旦松开左键，下一笔需要重新按 Ctrl 才能再次约束。

#### 5.2.5 两端自动添加球帽

为了让管道两端看起来"封口"而非"中空"，3D 画笔会 **自动在每一笔的两端添加球形帽**，使笔迹完整美观。你无需手动处理。

> 🧪 **练习 5.1**：用 2D 画笔在地面上画一个三角形，再用 3D 画笔在空中画一条 S 形曲线，观察两者的区别。

---

## 第六章 橡皮擦

本章教你使用橡皮擦删除对象或局部擦除笔迹。

### 6.1 删除对象模式（默认）

默认情况下，橡皮擦处于"删除整个对象"模式：

1. 在左面板工具区激活 **橡皮擦**。
2. 右面板显示橡皮擦属性，确认 **"2D 笔迹局部擦除" 复选框未勾选**。
3. 在画布上 **拖动橡皮擦圆圈**，让它碰到任何对象。
4. 一旦触碰，**整个对象** 立即被删除。

> ⚠️ **注意**：此模式下橡皮擦非常"霸道"，碰到什么就删什么，请谨慎使用。如果不小心误删，可立即按 **Ctrl + Z** 撤销。

### 6.2 2D 笔迹局部擦除

如果你只想擦除 2D 画笔笔迹的一部分（而不是整笔），可以开启局部擦除：

1. 激活橡皮擦。
2. 在右面板勾选 **"2D 笔迹局部擦除"** 复选框。
3. 此时橡皮擦对不同对象有不同行为：

| 对象类型 | 擦除行为 |
| --- | --- |
| **2D 画笔笔迹** | 局部擦除——橡皮擦与之相交的那一段被擦除，其余部分保留 |
| **3D 画笔 / 图形 / 文字 / 图片** | 删除最近的对象（与默认模式类似，但只删一个） |

> 💡 **小贴士**：局部擦除非常适合修正 2D 笔画的细节，比如把一条线的末端擦短一点，而不影响其它笔画。

### 6.3 调节橡皮擦大小

1. 激活橡皮擦。
2. 在右面板找到 **大小** 滑块。
3. 拖动滑块调整橡皮擦圆圈的半径：
   - 数值大 → 一次擦除范围广，适合大范围清理
   - 数值小 → 精细擦除，适合修细节

> 🧪 **练习 6.1**：用 2D 画笔画一条长线，然后用橡皮擦（开启局部擦除）在中间擦掉一小段，观察线的断裂效果。

---

## 第七章 文本和图片

本章教你如何在场景中添加文字和图片，丰富作品的信息量与表现力。

### 7.1 添加文本

#### 7.1.1 基本步骤

1. 在左面板工具区点击 **添加文本** 工具。
2. 在画布地面上 **单击** 你想放置文字的位置。
3. 弹出文本输入框，**输入文字内容**。
4. 确认后，文字会以 3D 形式出现在地面上。

#### 7.1.2 字体选择

右面板的文本框区提供 **8 种字体**：

| 字体 | 风格 |
| --- | --- |
| 雅黑 | 现代无衬线（默认） |
| 宋体 | 传统衬线 |
| 楷体 | 手写楷书 |
| 黑体 | 粗壮无衬线 |
| 仿宋 | 仿古衬线 |
| Arial | 西文无衬线 |
| Times | 西文衬线 |
| Courier | 西文等宽 |

**使用方法**：选中已有文本 → 在字体下拉框中选择 → 文字立即更换字体。

#### 7.1.3 字号

字号范围 **24 ~ 220**：

- 24：很小，适合标签
- 100：中等，适合标题
- 220：巨大，适合主视觉

**使用方法**：拖动字号滑块或直接输入数值。

#### 7.1.4 粗细

提供 **粗体 / 常规** 两种粗细：

- 粗体：笔画加粗，更显眼
- 常规：标准粗细

#### 7.1.5 排列方向

提供 **横排 / 竖排** 两种方向：

- 横排：文字从左到右排列
- 竖排：文字从上到下排列，适合古典风格

#### 7.1.6 双击编辑文本内容

1. 用选择工具 **双击** 文本对象。
2. 弹出编辑框，显示原文字。
3. 修改文字内容，确认即可。

> 💡 **小贴士**：双击编辑比删除后重新添加方便得多，记住这个快捷操作。

### 7.2 添加图片

#### 7.2.1 基本步骤

1. 在左面板工具区点击 **添加图片** 工具。
2. 弹出文件选择框，**选择本地图片**（推荐 JPG / PNG）。
3. 上传完成后，鼠标进入"放置模式"。
4. 在画布地面上 **单击** 放置图片。
5. 图片会作为平面贴片出现在地面上，可以像普通对象一样选中、移动、旋转。

> ⚠️ **注意**：图片是平面贴片，从背面看可能透明或反向，需要旋转视角观察。

### 7.3 图片贴图

除了把图片作为独立对象，你还可以把图片 **作为贴图** 应用到任何 3D 对象表面：

1. 选中目标对象（如立方体）。
2. 在右面板的 **花纹 / 贴图区** 找到 **上传图片贴图** 按钮。
3. 点击按钮，选择本地图片。
4. 图片会"贴"在对象表面，包裹住对象的各个面。

> 🧪 **练习 7.1**：放置一个立方体，添加文本"我的作品"设置为楷体、字号 100、竖排；然后给立方体贴上一张风景图片作为纹理。

---

## 第八章 视角控制

本章教你从不同角度观察场景，以及调整辅助显示元素。

### 8.1 7 种预设视角

底栏提供 7 种一键切换的预设视角：

| 视角 | 看到的效果 |
| --- | --- |
| **透视** | 默认的立体俯瞰视角，能看到三个面 |
| **顶视** | 从正上方往下看，只能看到地面（XY 平面） |
| **底视** | 从正下方往上看 |
| **正视** | 从正前方往后看 |
| **后视** | 从正后方往前看 |
| **左视** | 从正左侧往右看 |
| **右视** | 从正右侧往左看 |

**使用方法**：单击底栏对应按钮即可瞬间切换。

> 💡 **小贴士**：顶视图非常适合精确摆放平面位置；透视图适合整体审美观察。两者配合使用效率最高。

### 8.2 鼠标控制视角

除了预设视角，你还可以用鼠标自由控制：

| 操作 | 效果 |
| --- | --- |
| 长按右键拖动 | 旋转视角（环绕场景） |
| 长按滚轮拖动 | 平移视角（场景左右上下移动） |
| 滑动滚轮 | 缩放视角（拉近 / 推远） |

### 8.3 顶栏滚轮可缩放

在 **顶栏的非按钮区域** 滑动滚轮，也可以缩放视角。注意不要在按钮上滑动，以免误触。

### 8.4 坐标轴显示 / 隐藏

1. 在顶栏显示设置分组点击 **坐标轴** 按钮。
2. 坐标轴在"显示 / 隐藏"之间切换。
3. 隐藏后场景更简洁，适合截图出图；显示时便于判断方向。

### 8.5 网格面显示 / 隐藏

1. 在顶栏点击 **网格面** 按钮。
2. 地面的网格在"显示 / 隐藏"之间切换。
3. 隐藏后地面变为纯色，画面更干净。

### 8.6 全屏模式

1. 在顶栏点击 **全屏** 按钮，或按键盘 **F11** 键。
2. 浏览器进入全屏模式，画布占满整个屏幕。
3. 再次按 F11 或 Esc 退出全屏。

> 🧪 **练习 8.1**：切换到顶视图摆放几个对象，再切回透视图观察整体效果，最后隐藏坐标轴和网格面，全屏截图保存。

---

## 第九章 键盘快捷键

本章详细列出所有键盘快捷键，是提升效率的关键。建议把常用快捷键背下来。

### 9.1 基础按键

#### 9.1.1 选取对象时

| 按键 | 作用 |
| --- | --- |
| **方向键 ← →** | 沿 X 轴平移（左 / 右） |
| **方向键 ↑ ↓** | 沿 Y 轴平移（前 / 后） |
| **PgUp** | 沿 Z 轴升高 |
| **PgDn** | 沿 Z 轴降低 |
| **W / S** | 绕 X 轴旋转（前翻 / 后翻） |
| **A / D** | 绕 Y 轴旋转（左转 / 右转） |
| **Q / E** | 绕 Z 轴旋转（左倾 / 右倾） |
| **+ / -** | 等比放大 / 缩小 |

#### 9.1.2 未选取对象时

同样的方向键和 PgUp/PgDn 可用于 **微调视角** 或移动参考焦点；WASDQE 在未选取时通常无作用。

### 9.2 Shift 组合键（大步长）

按住 Shift 再按上述按键，会以 **更大的步长** 操作：

| 组合键 | 作用 |
| --- | --- |
| **Shift + 方向键** | 大步长平移（一次移动 1 个网格单位） |
| **Shift + W/S/A/D/Q/E** | 旋转 90°（一次转四分之一圈） |
| **Shift + +** | 放大 2 倍 |
| **Shift + -** | 缩小到 0.5 倍 |

> 💡 **小贴士**：Shift 组合键非常适合大幅调整，普通按键适合微调。两者结合使用能快速定位。

### 9.3 Ctrl 组合键（编辑操作）

| 组合键 | 作用 |
| --- | --- |
| **Ctrl + A** | 全选场景中所有对象 |
| **Ctrl + N** | 新建场景（清空当前） |
| **Ctrl + O** | 导入 JSON 文件 |
| **Ctrl + S** | 导出 JSON 文件 |
| **Ctrl + P** | 截图保存为 PNG |
| **Ctrl + Z** | 撤销上一步操作 |
| **Ctrl + Y** | 重做（恢复被撤销的操作） |
| **Ctrl + C** | 复制选中对象到剪贴板 |
| **Ctrl + V** | 粘贴剪贴板对象到原位置附近 |
| **Ctrl + D** | 克隆（原地复制）选中对象 |
| **Ctrl + Del** | 清空整个场景 |

### 9.4 功能键

| 按键 | 作用 |
| --- | --- |
| **Esc** | 取消当前选择 / 退出当前模式 |
| **F11** | 切换全屏模式 |
| **Del** | 删除当前选中的对象 |

> 📌 **记忆技巧**：Ctrl 系列与 Windows 通用快捷键高度一致（复制粘贴撤销重做等），只是把"保存"换成了"导出 JSON"，把"打印"换成了"截图"。

---

## 第十章 复制、克隆与历史

本章教你高效地重复创作，以及如何回退操作。

### 10.1 复制（Ctrl + C）

1. 选中一个或多个对象。
2. 按 **Ctrl + C**（或点击顶栏"复制"按钮）。
3. 选中对象的数据被复制到剪贴板，原对象保持不变。

### 10.2 粘贴（Ctrl + V）

1. 在复制之后，按 **Ctrl + V**（或点击顶栏"粘贴"按钮）。
2. 剪贴板中的对象会被粘贴到 **原位置附近**（略微偏移，便于区分）。
3. 粘贴出来的对象自动处于选中状态，可立即移动。

> 💡 **小贴士**：可以连续按 Ctrl + V 多次粘贴，快速生成一排相同的对象。

### 10.3 克隆（Ctrl + D）

1. 选中对象。
2. 按 **Ctrl + D**（或点击顶栏"克隆"按钮）。
3. 会在 **原位置** 立即复制一个完全相同的对象，并自动选中克隆体。

> 📌 **复制 vs 克隆的区别**：
> - **复制 + 粘贴**：两步操作，粘贴位置略微偏移。
> - **克隆**：一步操作，原地复制，需要手动移开才能看到原对象。

### 10.4 撤销（Ctrl + Z） / 重做（Ctrl + Y）

- **Ctrl + Z**：撤销上一步操作，可连续按多次回退多步。
- **Ctrl + Y**：重做被撤销的操作，可连续按多次恢复多步。

> ⚠️ **注意**：撤销 / 重做的历史步数有一定上限，超早期操作可能无法撤销。重要操作前建议先导出 JSON 备份。

### 10.5 清空场景

1. 按 **Ctrl + Del**（或点击顶栏"清空"按钮）。
2. 弹出确认提示（如有）。
3. 确认后，场景中所有对象被删除，恢复到空场景。

> ⚠️ **重要**：清空操作不可轻易撤销，请务必先导出 JSON 备份当前作品！

---

## 第十一章 文件操作

本章教你管理你的作品文件——新建、导入、导出、截图。

### 11.1 新建场景（Ctrl + N）

1. 按 **Ctrl + N**（或点击顶栏"新建"按钮）。
2. 如果当前场景有未保存内容，建议先导出。
3. 新建后场景被清空，进入空白状态。

### 11.2 导入 JSON（Ctrl + O）

本软件使用 **JSON 格式** 保存场景数据，方便分享和备份。

1. 按 **Ctrl + O**（或点击顶栏"导入"按钮）。
2. 弹出文件选择框，选择一个之前导出的 `.json` 文件。
3. 场景会被替换为该文件的内容。

> 💡 **小贴士**：JSON 文件是纯文本格式，可以用记事本打开查看，但请勿随意修改以免无法导入。

### 11.3 导出 JSON（Ctrl + S）

1. 按 **Ctrl + S**（或点击顶栏"导出"按钮）。
2. 浏览器自动下载一个 `.json` 文件，文件名通常包含时间戳。
3. 把文件保存到安全位置，作为你作品的备份。

> 📌 **建议**：每完成一个阶段就导出一次，养成勤保存的好习惯，避免意外丢失。

### 11.4 截图 PNG（Ctrl + P）

1. 调整好你想要截取的视角和画面。
2. 按 **Ctrl + P**（或点击顶栏"截图"按钮）。
3. 浏览器自动下载一张 **PNG 图片**，记录当前画布画面。
4. 截图前建议隐藏坐标轴和网格面，让画面更干净。

> 🧪 **练习 11.1**：完成一个小作品后，先导出 JSON 备份，再截图保存为 PNG，最后新建场景，再导入刚才的 JSON 验证备份是否有效。

---

## 第十二章 高级技巧

本章介绍一些进阶用法，帮助你更高效、更专业地使用本工具。

### 12.1 多选整体操作

- 用 Shift + 点击或框选选中多个对象后，**所有变换操作都是整体进行**。
- 整体平移：保持相对位置一起移动。
- 整体旋转：以选择组中心为轴旋转。
- 整体缩放：以选择组中心为基准同步缩放。
- 整体改色：所有选中对象同时应用颜色 / 花纹。

> 💡 **应用场景**：建造一排房屋、一片树林、一组家具时，先做好一个，再用复制 + 整体变换快速排列。

### 12.2 面板折叠与展开

- 左面板和右面板都可以 **折叠 / 展开**。
- 点击面板边缘的折叠按钮，面板会收起，让画布获得更大空间。
- 再次点击可重新展开。
- 底栏同样支持折叠。

> 💡 **小贴士**：在窄屏设备上，面板会自动折叠，需要时再展开。

### 12.3 窄屏适配

当窗口宽度较小时：

- 左右面板会 **自动折叠** 以保证画布可见。
- 顶栏按钮区可 **横向滚动**，所有按钮仍然可访问。
- 触摸手势会自动启用，方便手机 / 平板操作。

> 📌 **建议**：在桌面宽屏下使用体验最佳；移动端适合快速查看和简单编辑。

### 12.4 触摸设备使用

在触摸屏上：

- 用 **单指拖动空白** 旋转视角（替代鼠标右键）。
- 用 **双指拖动** 平移视角（替代滚轮按住）。
- 用 **双指捏合** 缩放视角（替代滚轮滑动）。
- **单指轻点空白** 可取消选择。

> ⚠️ **注意**：触摸操作没有"右键"概念，所以视角旋转改为单指拖动空白处。

### 12.5 动态网格自适应

- 场景的网格会根据视角和缩放 **自适应调整密度**。
- 缩放越近，网格越细密；缩放越远，网格越粗大。
- 这样无论放大还是缩小，都能看到合适的网格参考。

### 12.6 对象渲染顺序与透明度

- 不透明对象按"从前到后"渲染，保证遮挡正确。
- 半透明对象按"从后到前"渲染，保证混合正确。
- 当多个半透明对象互相重叠时，可能出现 **渲染顺序不准确** 的现象（背后对象透过前面显出来）。
- 解决方法：
  1. 尽量减少半透明对象的重叠。
  2. 调整透明度到 100% 让对象不透明。
  3. 微调对象位置避免完全重叠。

> 📌 **原理**：这是实时 3D 渲染的固有限制，并非软件 bug。理解这一点能帮助你避免设计上的"视觉陷阱"。

---

## 附录 A 常见问题

### Q1：打开网页是空白，什么都没有？

- 确认本地服务已启动，且端口为 8090。
- 尝试刷新页面（F5 或 Ctrl + R）。
- 更换为 Chrome 或 Edge 浏览器。
- 打开浏览器控制台（F12）查看是否有报错。

### Q2：放置的图形看不见？

- 检查视角是否朝向了对象的反面，尝试旋转视角。
- 检查对象透明度是否被设为 0%。
- 检查对象是否被其他对象遮挡，尝试切换到顶视或正视查看。

### Q3：拖动控件时对象乱跑？

- 确认你拖动的是 **正确的轴**（红=X、绿=Y、蓝=Z）。
- 尝试切换到正交视角（如正视）再操作，会更直观。
- 用属性面板直接输入数值，最精确。

### Q4：撤销不管用？

- 撤销有历史步数上限，可能已超出范围。
- 某些操作（如视角调整）不会进入撤销历史。
- 建议重要操作前先导出 JSON 备份。

### Q5：导入 JSON 失败？

- 确认文件是本软件导出的 JSON 格式。
- 用记事本打开文件，检查是否为有效的 JSON 文本。
- 不要手动修改 JSON 内容，以免破坏结构。

### Q6：3D 画笔画的管道位置不对？

- 3D 画笔在 **参考平面** 上绘制，参考平面由首次点击确定。
- 想画水平曲线，先切到顶视再点击确定平面。
- 想画垂直曲线，先切到正视或侧视再点击确定平面。

### Q7：触摸屏上没法右键旋转视角？

- 触摸设备没有"右键"概念。
- 用 **单指拖动空白处** 即可旋转视角。
- 用 **双指拖动** 平移，**双指捏合** 缩放。

### Q8：半透明对象看起来很奇怪？

- 这是 3D 渲染的透明排序限制。
- 尽量避免多个半透明对象互相重叠。
- 详见 [第十二章 12.6 节](#126-对象渲染顺序与透明度)。

---

## 附录 B 速查表

### 鼠标速查

| 操作 | 鼠标 | 触摸 |
| --- | --- | --- |
| 选择对象 | 单击左键 | 单指单击 |
| 编辑文本 | 双击左键 | 单指双击 |
| 操作对象 / 控件 | 长按左键拖动 | 单指拖动对象 |
| 旋转视角 | 长按右键拖动 | 单指拖动空白 |
| 平移视角 | 长按滚轮拖动 | 双指拖动 |
| 缩放视角 | 滑动滚轮 | 双指捏合 |
| 框选 | 空白处长按左键拖动 | — |
| 取消选择 | — | 单指轻点空白 |

### 坐标系速查

| 轴 | 颜色 | 方向 |
| --- | --- | --- |
| X | 红 | 左右 |
| Y | 绿 | 前后 |
| Z | 蓝 | 上下 |

### 快捷键速查

| 类别 | 快捷键 | 作用 |
| --- | --- | --- |
| 平移 | 方向键 / PgUp / PgDn | X/Y/Z 轴移动 |
| 旋转 | W A S D Q E | 三轴旋转 |
| 缩放 | + / - | 等比缩放 |
| 大步长 | Shift + 上述键 | 大幅调整 |
| 全选 | Ctrl + A | 选中所有对象 |
| 新建 | Ctrl + N | 新场景 |
| 导入 | Ctrl + O | 导入 JSON |
| 导出 | Ctrl + S | 导出 JSON |
| 截图 | Ctrl + P | 保存 PNG |
| 撤销 | Ctrl + Z | 回退一步 |
| 重做 | Ctrl + Y | 恢复一步 |
| 复制 | Ctrl + C | 复制到剪贴板 |
| 粘贴 | Ctrl + V | 粘贴 |
| 克隆 | Ctrl + D | 原地复制 |
| 清空 | Ctrl + Del | 清空场景 |
| 取消 | Esc | 取消选择 |
| 全屏 | F11 | 切换全屏 |
| 删除 | Del | 删除选中对象 |

### 视角速查

| 视角 | 用途 |
| --- | --- |
| 透视 | 整体观察 |
| 顶视 | 精确摆位 |
| 底视 | 仰视检查 |
| 正视 / 后视 | 正面 / 背面观察 |
| 左视 / 右视 | 侧面观察 |

---

## 结语

恭喜你读完了整本说明书！现在你已经掌握了 **立方·三维设计工坊** 的全部功能。

记住，工具只是手段，创意才是核心。从今天放置第一个立方体开始，不断尝试、不断组合、不断突破，你一定能创造出独一无二的三维作品。

如果你在使用过程中遇到本说明书未涵盖的问题，欢迎：

1. 重新阅读相关章节，寻找细节。
2. 查阅附录 A 常见问题。
3. 多动手实验，软件的所有操作都可以用 **Ctrl + Z** 撤销，不必害怕犯错。

祝你创作愉快！

---

*本说明书由 yushichadao 编写，适用于"立方·三维设计工坊"软件。*
